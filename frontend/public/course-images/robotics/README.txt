Course photographs for the robotics lessons.

One folder per lesson id; filenames are named by the lesson JSON. Until a file
exists the lesson shows a placeholder card describing the photo that belongs
there, so adding an image means saving it at the right path and nothing else.

The full list of what is still missing: docs/robotics-images.md
